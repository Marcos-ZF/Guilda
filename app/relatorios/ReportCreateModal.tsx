"use client";
import {useRef} from "react";import {createReport} from "./actions";import styles from "./ReportCreateModal.module.css";
type Employee={id:string;code:string;name:string};
export default function ReportCreateModal({employees,today}:{employees:Employee[];today:string}){const dialog=useRef<HTMLDialogElement>(null);return <><button className={styles.open} type="button" onClick={()=>dialog.current?.showModal()}>Novo relatório</button><dialog className={styles.modal} ref={dialog} onClick={event=>{if(event.target===dialog.current)dialog.current.close()}}><div className={styles.panel}><div className={styles.header}><div><small>Novo registro</small><h2>Criar relatório</h2></div><button type="button" onClick={()=>dialog.current?.close()} aria-label="Fechar janela">×</button></div><form className={styles.form} action={createReport}><label>Nome<input name="title" maxLength={140} required autoFocus/></label><label>Data<input name="report_date" type="date" defaultValue={today} required/></label><label className={styles.wide}>Resumo<textarea name="summary" maxLength={5000} required/></label><label className={styles.wide}>Link do Doc<input name="document_url" type="url" placeholder="https://docs.google.com/..." required/></label><label className={styles.wide}>Quem fez<select name="author_employee_id" required defaultValue=""><option value="" disabled>Selecione um funcionário</option>{employees.map(e=><option value={e.id} key={e.id}>{e.name} · {e.code}</option>)}</select></label><div className={styles.actions}><button className={styles.cancel} type="button" onClick={()=>dialog.current?.close()}>Cancelar</button><button type="submit">Criar relatório</button></div></form></div></dialog></>}
